// Split - takes a string and turns it into an array
// Join - takes and array and turns it into a string
// Slice and splice - insert and cut things from any part an array. *Splice will actually change the original data while slice does not